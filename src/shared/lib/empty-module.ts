// Empty stub used to satisfy bundlers when client code statically references
// Node-only modules (e.g. highs-wasm references `fs`/`path` inside a
// runtime-guarded branch that never executes in the browser).
export default {};
